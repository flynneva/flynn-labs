#env: standard
runtime: nodejs10
#service: default

handlers:
  - url: /sports/.*
    scripts: auto

  - url: /(.*\.(ico|js))$
    static_files: build/\1
    upload: build/.*\.(ico|js)$

  - url: .*
    static_files: build/index.html
    upload: build/index.html
